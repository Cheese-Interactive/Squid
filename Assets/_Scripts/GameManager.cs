using UnityEngine;

public class GameManager : MonoBehaviour {
    [SerializeField] private Pickup pointsPrefab;
    [SerializeField] private Pickup matsPrefab;
    // Start is called before the first frame update
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }
}
